/*
    !
    NOT logical operaros
    typlcally used to reverse a codition's value
    true -> false false -> true 
 */

    let temp = 15;
    let surya = false;
 //    let surya = true;

    if(!(temp > 0)){
     console.log("it's cold outside");
    }
    else{
     console.log("it's warm outside");
    }
    if(!(surya)){
        console.log("it's cloudy outside");
    }
    else{
        console.log("it's surya outside");
       }
